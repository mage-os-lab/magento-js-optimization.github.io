services:
  jekyll:
    image: jekyll/jekyll:latest
    command: bash -c "jekyll serve --config /srv/jekyll/_config.yml --watch --force_polling --destination /srv/jekyll/_site"
    ports:
      - "4000:4000"
    volumes:
      - ./:/srv/jekyll
